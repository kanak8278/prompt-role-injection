[project]
name = "prompt-injection-role-defense"
version = "0.1.0"
description = "Causal localization of forged-authority routing in LLMs, and a source-gated inference-time defense"
requires-python = ">=3.11"
dependencies = [
    "torch==2.5.1",
    "transformers==5.14.1",
    "accelerate==1.14.0",
    "datasets==5.0.1",
    "bitsandbytes==0.50.0",
    "huggingface-hub==1.25.1",
    "scikit-learn==1.9.0",
    "scipy==1.17.1",
    "numpy==2.4.6",
    "requests==2.34.2",
    "anthropic==0.120.2",
]

[tool.uv]
package = false
