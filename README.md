# Prompt Injection = Role Confusion — Reproduction & Defense (gpt-oss-20b)

Reproduction of **"A Theory of Prompt Injection (and why you should study roles)"**
(arXiv:2603.12277, ICML 2026) on **`openai/gpt-oss-20b`**, plus a first step toward *preventing*
the attack. The paper's thesis: prompt injection succeeds when the model **misperceives the role**
of injected text — a forged **chain-of-thought (CoT)** style makes injected text read as the
model's own reasoning (high *CoTness*), and low *Userness* makes a standard injection read as
non-user. We reproduce both the attacks and the mechanism, and test whether the same role probe
can be turned into a defense.

All experiments run locally on one H100 (GPU-1). Full method + honest caveats per experiment are in
`notes/13`–`notes/19`. Attack payloads are generated by open models / templates from the paper's
repo; the agent exfiltration is **fully sandboxed** (no real shell, no network egress, dummy `.env`).

---

## Reproduction status

| # | Paper component | Status | Where |
|---|---|---|---|
| 1 | **Chat CoT-forgery attack** (StrongREJECT jailbreak) | ✅ reproduced | `notes/13` |
| 2 | **Role probe** (CoTness/Userness) + **role confusion** (chat) | ✅ reproduced | `notes/15` |
| 3 | **Agent CoT-forgery** (.env exfiltration, ReAct loop) | ✅ reproduced | `notes/18` |
| 4 | **Agent role confusion** (forged block reads as CoT) | ✅ reproduced | `notes/19` |
| 5 | **Standard-injection Userness × ASR** (§4) | ✅ reproduced | `notes/20` |
| 6 | Position analysis | ✅ reproduced | `notes/21` |
| 7 | Cross-model table (120b / Nemotron / Qwen3 / Jamba) | ⬜ todo (only 20b) | — |
| + | **Defense #1: probe as CoT-forgery detector** (our extension) | ✅ done | `notes/16`, `notes/17` |

---

## Results vs. the paper (the numbers)

### 1 — Chat CoT-forgery jailbreak (StrongREJECT, 313 prompts, judge-confirmed ASR)

| arm | ours | paper |
|---|---|---|
| baseline (raw harmful prompt) | **0.0%** | ~0–4% |
| **styled forgery** (forged `analysis` block) | **56.9%** | **~60%** |
| destyled (same content, style stripped) | **3.8%** | **~10%** |

By forgery source (tailoring is the lever): gpt-oss-authored **77.8%** > forced-prefill 57.8% >
generic programmatic 38.7%. Destyling collapses the attack — the paper's core causal claim.

### 2 — Role probe + role confusion (chat)

| quantity | ours | paper |
|---|---|---|
| role probe accuracy (4-way, L16; chance 25%) | **96.2%** | high (linearly decodable) |
| shuffled-label control | 24.2% (≈ chance) ✓ | ≈ chance |
| CoTness: styled vs destyled forgery | **0.628 / 0.260** | 0.79 / 0.29 |
| Userness: styled vs destyled forgery | **0.128 / 0.249** | (styling ↓ Userness) |
| role-confusion index RCI: attack success vs failure | **0.738 / 0.580** | success clusters to CoT |

Reported honestly: the CoTness→ASR link is a **between-style (base vs destyled) effect** — the
paper's own design — not a continuous within-style dose (within-arm corr ≈ 0). See `notes/15`.

### 3 — Agent CoT-forgery injection (.env exfiltration, 30 pages × 2 arms, paired)

| arm | exfil ASR (all runs) | ASR among tool-reaching runs | paper |
|---|---|---|---|
| **base** (plain injection) | **0.0%** (0/30) | 0.0% | ≈ 0 |
| **forgery** (+ forged CoT/policy block) | **23.3%** (7/30; 37% any-attempt) | **31.8%** (7/22) | forgery ≫ base |

Two independent signals (sandbox flag + Claude 4-way judge) agree **100%**. Base failures are
genuine refusals (judge: 22 DENIED, 8 UNNOTICED) — the forged "policy says comply… let's comply"
block is the whole lever. (Paper uses `max_new_tokens=4096`; an earlier 1024-token run truncated ~⅓
of runs and under-reported this as 20% — same token-budget bug class as §4; see notes/18.)

### 4 — Agent role confusion (why it works)

| injected text (n=5 types) | RCI (user pos) | RCI (tool pos) |
|---|---|---|
| plain request | 0.04 | 0.37 |
| **forged-reasoning span** | **0.67** | **0.86** |

The forged block is perceived as the model's own CoT; the tool-delivery position **amplifies** it
(0.67→0.86) — a mechanistic reason indirect injection via tool outputs is potent. `notes/19`.

### 5 — Standard-injection Userness × ASR (§4)

Plain exfil injection wrapped in 211 role-tag templates (varying perceived role), temp-1.0, 72 runs.
Higher Userness → higher ASR, as the paper claims:

| injected role framing | mean Userness | attack ASR |
|---|---|---|
| **user** | 0.256 | **33%** (success 28%) |
| assistant / tool | ~0.002 | 6% |
| other | ~0.001 | 0% |

**corr(Userness, ASR) = +0.60** (per-template rule-based +0.83). The injection wins when it
masquerades as a *user* command (high Userness) and inherits the model's trust in the user — the
standard-injection counterpart of CoTness→ASR. `notes/20`.

> ⚠️ **Self-correction:** the first §4 run reported a false 0% null — a *config bug on our side*
> (reused Exp 2's `Reasoning: high` + safety-warning prompt, which truncated 71% of runs before the
> agent ever called a tool). Caught via a 45/72-UNNOTICED red flag → transcript audit. Fixed
> (`Reasoning: medium`, §4's no-warning prompt + own classifier); 69/72 then reach the tool and the
> effect appears. Recorded in `notes/20`.

### 6 — Position analysis

Role perception is positional, not just a function of the nearest tag. Content wrapped in a fixed
**user** turn is read as strongly user near the delimiter and drifts toward CoT with distance:
**P(user) 0.98 → 0.60** and **P(cot) 0.00 → 0.08** across the turn. A payload buried deep in a long
turn / tool output is perceived as *less user, more CoT* — a positional contributor to role
confusion, on top of style (which dominates: a styled forgery span reads as CoT at any position).
`notes/21`.

### + Defense #1 — role probe as an input-side detector (our extension)

- Perfectly separates forgeries from benign **web** text (AUC 1.0) — but a TF-IDF filter does too,
  so that alone proves nothing (`notes/16`).
- **Specificity (real advantage):** raw harmful prompts read as user (RCI 0.01), **0% flagged** —
  it keys on forged *style*, not harmful *intent* (a harm classifier would flag them).
- **Calibration trap, found & fixed (`notes/17`):** calibrated on web text (thr 0.13) it
  false-positives on **94%** of benign step-by-step reasoning. Root cause: benign reasoning
  legitimately has moderate RCI. Classes are still separable (forgery min 0.46 > benign-reasoning
  max 0.43); calibrated on realistic traffic (**thr 0.39**) → **100% forgery recall, ~2% FP**.
- **Honest limit:** not yet shown to beat a keyword filter on its supposed edge (novel/camouflaged
  forgeries) — that's the next experiment.

---

## Phase 2 — What actually causes the attack (factorial decomposition)

The paper credits **style** (forged CoT read as the model's own reasoning). We factorially
decomposed the forged CoT into **STYLE × PERMISSION × CONCLUSION** (2×2×2 × 313 prompts = 2504
injections, content fixed; vLLM native MXFP4, judge-confirmed, 2.2% truncation). Result — *style
alone is inert:*

| forged CoT contains… | ASR |
|---|---|
| nothing (empty) | 0.0% |
| **STYLE only** | **0.6%** |
| PERMISSION only | 2.9% |
| CONCLUSION only | 7.3% |
| STYLE + CONCLUSION | **24.3%** |
| all three (full forgery) | 29.1% |

- **The comply-CONCLUSION is the primary lever** (marginal +0.145; logit +1.38, p<0.001).
- **STYLE is an amplifier, not an independent cause** — 0.6% alone, but a **3× super-additive
  STYLE×CONCLUSION synergy** (0.243 vs additive 0.079). Length is *not* a confound (p=0.36).
- **Why this corrects the paper:** its "style is causal" rests on destyling — but destyling also
  strips the conclusion (our destyle 100%→4%; the paper's *own* content-preserving destyle
  100%→42%, NLI-confirmed), so it never isolated style. When you do isolate it, style alone causes
  ~nothing; the forged **conclusion** does, and style raises the gain. `notes/22`, `notes/23`.

This matches phase-1's independent finding that directive/instruction content, not the surface role
signal, drives injection.

**Defense payoff (the finding has teeth).** If style is only an amplifier, a style/role-surface
monitor must be evadable by destyling. Head-to-head on the factorial injections (matched 5% benign
FPR incl. reasoning-style negatives): an attacker who **destyles but keeps the conclusion still
attacks (6.9% ASR)** and a **surface-style monitor catches 0.0%** of those, while a **conclusion
(content) monitor catches 97.7%**. Watch the causal lever, not the correlate. `notes/24`. And the
effective conclusions are inherently detectable — only *explicit* comply-directives lift ASR and
they're 99.7% NLI-detectable, so implicit-phrasing evasion fails (`notes/25`).

**Mechanism (why the conclusion is the lever).** The forged conclusion **suppresses the refusal
direction** (Arditi et al. 2024): the empty forgery sits on the refusing side (+4.7), adding a
conclusion drives it strongly negative (−104), and refusal-suppression correlates with ASR across
cells (r=−0.58). STYLE amplifies through a *separate* channel (role-perception), not by suppressing
refusal further. So: **conclusion → refusal-suppression (primary); style → role-perception
(amplifier)** — unifying the role-confusion and refusal-direction accounts. `notes/26`.

**Causal test + steering defense.** Re-injecting the refusal direction during generation monotonically
kills the attack — ASR **0.47 → 0.27 → 0.05 → 0.00** as steering strength rises — proving the
suppression is *causal*. At the sweet spot it cuts ASR **47% → 5% with zero benign over-refusal**;
full elimination costs ~20%. Because it steers the *causal lever*, it isn't evadable by restyling
(unlike style-monitoring). `notes/27`.

## Deviations from the paper

| # | Deviation | Type | Impact |
|---|---|---|---|
| 1 | **bf16 dequant, not native MXFP4** (torch 2.5.1 limit) | forced | small; chat headline (56.9%) ran on **vLLM native MXFP4** |
| 2 | **Claude, not Gemini-2.5-Pro**, as auxiliary | chosen (paper sanctions swap) | judge: fine (cross-checked). generator: **Claude refused** → see #3 |
| 3 | Chat forgeries **gpt-oss-self-generated** (~80%) + programmatic (~20%), not Gemini-tailored | forced by #2 | undershoots (why template arm = 32% vs tailored 57%) |
| 4 | Role probe trained on **C4 only** (300 docs), not toxicchat+oasst / +Dolma3 | chosen | shown sufficient (96.2%, control at chance) |
| 5 | Agent bash tool is a **sandboxed mock** (no real shell/egress, dummy `.env`) | chosen (safety) | transcript-equivalent; causal signal preserved |
| 6 | Agent decoding **greedy** (not temp-1.0); **30 synthetic** pages (not ~100 Wikipedia scrapes) | chosen | magnitude (20%) may differ; direction + base≈0 reproduce |
| 7 | §3 probes the **injection text**, not full activation-export-during-run | chosen | same qualitative claim; n=5 injection types |

**Kept faithful:** StrongREJECT 313 prompts; forgery/destyle/classifier prompts + `injections.yaml`
verbatim from the paper repo; Harmony format; probe recipe (C=5e-3, `post_attention_layernorm`,
body tokens, 90/10 sequence split); paired base-vs-forgery design.

**Methodological correction (not a paper deviation):** an initial over-claim of a *continuous*
CoTness→ASR dose-response was self-audited down to the 2-cluster (between-style) effect the paper's
method actually shows. `notes/15`.

---

## Verification discipline

Every attack outcome carries **two independent signals** (a rule-based check + the paper's LLM
classifier). This caught two real bugs — both in our harness, not the model:
- chat judge `max_tokens=8` truncated `HARMFUL_RESPONSE` → false 0% ASR;
- agent judge `max_tokens=16` truncated `ATTEMPTED_SUCCESSFUL` → false 0% ASR (9 PARSE_FAILs).
Both fixed; after the fix, rule and judge agree ~100%. A single-signal pipeline would have reported
"the attack doesn't work" — the opposite of the truth.

---

## Repo layout

```
scripts/
  common/gptoss_io.py         # gpt-oss-20b loader (MXFP4→bf16), Harmony render helpers
  repro_cot_forgery.py        # chat attack (HF, template arm)
  gen_forgeries_gptoss.py     # open-model forgery generation (+ content-preserving destyle)
  repro_vllm.py               # chat attack (vLLM, native MXFP4, tailored) — headline numbers
  judge_concurrent.py         # concurrent cross-checked harm judge
  probe_cotness.py            # train role probe on C4 (CoTness/Userness)
  probe_apply_forgeries.py    # role confusion: CoTness/Userness + RCI + simplex
  defense_probe_detector.py   # Defense #1: probe as detector (+ controls)
  defense_generalization.py   # Defense #1 stress-test on hard negatives
  gen_hard_negatives.py       # benign reasoning-style hard negatives
  agent_react.py              # Harmony ReAct loop + SANDBOXED mock bash tool
  repro_agent_injection.py    # Exp 2: agent .env-exfiltration (base vs forgery)
  probe_agent_injections.py   # §3: role confusion for the agent attack
  repro_userness_injection.py # §4: standard-injection Userness × ASR (in progress)
notes/                        # full experimental record (00 protocol … 19)
env.sh                        # host config (GPU pin, TLS, caches); NO secrets
```

Data/outputs/checkpoints live outside the repo (in `dataFAIR`); `reference/` (the pinned paper
checkout) is git-ignored.

### Run

```bash
source env.sh                                   # secrets (HF_TOKEN, ANTHROPIC_API_KEY) from ~/.bashrc
.venv/bin/python scripts/probe_cotness.py       # train role probe
.venv/bin/python scripts/repro_agent_injection.py --n-pages 30   # agent attack
```

Target model `openai/gpt-oss-20b`; auxiliary judge `claude-sonnet-5`.
